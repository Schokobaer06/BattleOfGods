package com.schokobaer.battleofgods.tag;

/**
 * Creates tags for every main class, subclass, tier, rarity
 */

public class TagCreator {
    /*
    //private static final Map<String, TagKey<?>> tags = new HashMap<>();

    public static TagKey<MainClass> createMainClassTag(@NotBlank String name) {
        TagKey<MainClass> tag = TagKey.create(InitMainClass.MAIN_CLASSES.getRegistryKey(), new ResourceLocation(BattleOfGods.MODID, name.toLowerCase()));
        //tags.put(name.toLowerCase(), tag);
        return tag;
    }

    public static TagKey<ItemOverride> createSubClassTag(@NotBlank String name, RegistryObject<MainClass> mainClass) {
        TagKey<ItemOverride> tag = TagKey.create((net.minecraft.resources.ResourceKey<? extends net.minecraft.core.Registry<ItemOverride>>) (Object) ForgeRegistries.ITEMS.getRegistryKey(), new ResourceLocation(BattleOfGods.MODID, name.toLowerCase()));
        //tags.put(name.toLowerCase(), tag);
        return tag;
    }

    public static TagKey<Tier> createTierTag(String name) {
        TagKey<Tier> tag = TagKey.create(InitTier.TIERS.getRegistryKey(), new ResourceLocation(BattleOfGods.MODID, name.toLowerCase()));
        //tags.put(name.toLowerCase(), tag);
        return tag;
    }

    public static TagKey<Rarity> createRarityTag(String name) {
        TagKey<Rarity> tag = TagKey.create(InitRarity.RARITIES.getRegistryKey(), new ResourceLocation(BattleOfGods.MODID, name.toLowerCase()));
        //tags.put(name.toLowerCase(), tag);
        return tag;
    }
     */
}
